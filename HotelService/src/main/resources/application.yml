eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    preferIpAddress: true
server:
  port: 8085
spring:
  application:
    name: Hotel-Service
ribbon:
  eureka:
    enabled: true